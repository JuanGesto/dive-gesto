import React from "react";

const ItemListContainer = ({ saludo }) => {
    return (
        <>
            <section id="featured">
                <h2>{saludo}</h2>
            </section>
        </>
    );
};

export default ItemListContainer;
